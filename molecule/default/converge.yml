---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Print OS information
      ansible.builtin.debug:
        msg: "Running on {{ ansible_distribution }} {{ ansible_distribution_version }} ({{ ansible_distribution_release }})"
